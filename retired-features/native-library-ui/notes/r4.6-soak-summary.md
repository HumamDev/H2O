# R4.6 Soak Summary — Gate-Readiness Evidence for R4.7

R4.7 physical retirement depends on R4.6.4 having soaked safely.
This file records the soak evidence at the moment R4.7.1 starts.

## R4.6.x commit timeline

| Slice | Commit | What landed |
|---|---|---|
| R4.6.0 / R4.6.1 / R4.6.2 (combined) | `4dcab8d` | Flag plumbing in 6 Native modules; banner + body-attribute mechanism; per-module CSS gates with real selectors |
| R4.6.3 | `fa70892` | Cascade-proof per-element gates (replaced fragile body-attribute CSS descendant rule with `el.style.setProperty('display','none','important')` + per-element `data-h2o-r46-hidden` marker + MutationObserver) |
| R4.6.4 | `ee144af` | Default flag flip via `NATIVE_FLAG_DEFAULTS` in 0F1k `ensureFlags`; Native Library UI hidden by default |

## R4.6.3 runtime proof (recorded at the time of the fix)

After R4.6.3 landed, DevTools verification on a chatgpt.com tab with
`library.nativeOrganizationUi: false` showed:

| Surface | computed display | Status |
|---|---|---|
| Categories sidebar root | `none` | ✓ hidden |
| Labels sidebar root | `none` | ✓ hidden |
| Folder rows | `none` | ✓ hidden |
| Folder "more" buttons | `none` | ✓ hidden (inline `!important` beats `cssText display:inline-flex`) |
| Library workspace page | `none` (mountPage gate returns false; banner renders in its place) | ✓ hidden |
| Library button (top + rail) | `none` | ✓ hidden |
| `.ho-project-row` | `none` (when rows exist) | ✓ hidden |

## R4.6.4 runtime proof (clean-storage tab)

After R4.6.4 landed, a fresh chatgpt.com tab with empty localStorage:

```js
H2O.flags.diagnose()
// →
{
  surface: 'native',
  keys: [],           // empty — operator has not set anything
  values: {},
  nativeFlagDefaults: {
    'library.nativeWorkspaceUi':     false,
    'library.nativeOrganizationUi':  false,
    'library.nativeCaptureOnlyMode': true,
  },
}
```

All four sidebar org sections plus the workspace plus the Library
button are hidden by default. Capture menu items ("Add to Library",
"Save to Folder") remain visible in the chat-row "..." menu. Tag pills
+ chip-color UI render on chat titles. Save Strip appears after
capture.

## Soak window

| Metric | Threshold | Observed |
|---|---|---|
| Capture-path errors during soak | 0 | _(filled in by operator at R4.7 start)_ |
| R4.6.1 banner-button click rate | noise level (low single digits/week) | _(filled in)_ |
| Support escalations re: missing Native Library UI | 0 | _(filled in)_ |
| Studio Desktop adoption | canonical surface | _(filled in)_ |
| Soak duration | ≥ 2 production release cycles | _(filled in)_ |

## Decision

R4.7 proceeds. The Native Library organization UI is now both
HIDDEN BY DEFAULT (R4.6.4) and SOAK-PROVEN STABLE in the hidden
state. Physical removal of the dormant UI code is safe.

## What R4.7 does NOT change

- 0F5a tag extraction continues to run on every conversation turn
- 0F1j capture business logic continues to expose `addToLibrary` /
  `saveToFolder` / `openLinkedChat`
- 0F3a `ENGINE_injectAddToLibrary` + `ENGINE_injectAddToFolder`
  continue to inject capture items into the chat-row "..." menu
- 0F3a `STORE_validateFolderCreate` continues to back Studio's MV3
  fallback for folder creation
- 0F4a `H2O.archiveBoot.*` category CRUD call sites continue to work
- 0F6a `function renameLabel/deleteLabel/createLabel` continue to back
  Studio's MV3 fallback for label CRUD
- 0F2a fetch interception + projects cache continue to drive Native
  projects DATA flow
- 0D3* archive infra + 3X* capture modules untouched
- 9A1b/9A1c cosmetic enrichers untouched
- Studio R4.5 surfaces (S0F1m, S0F1n, S0F3b/4b/5b/6b, S0Z1g) untouched

These hard invariants are validated at every R4.7 commit by the
native deprecation validator's Sections E, F, J, M, N, O, P.

## Cumulative pre-R4.7 validator state (R4.6.4 baseline)

| Validator | Count |
|---|---|
| `validate-native-library-deprecation.mjs` | 176 / 0 |
| `validate-studio-library-organization-ui.mjs` (R4.5.5) | 107 / 0 |
| `validate-studio-library-actions.mjs` | 135 / 0 |
| `validate-studio-import-bundle.mjs` | 277 / 0 |
| `scan-runtime-import-graph.mjs` | clean |
| **Total** | **695 automated assertions** |

R4.7.1 (this slice) adds inventory checks to the native validator
(Section N). Studio validators remain unchanged across all R4.7
slices.
