/** Target rail width as a fraction of screen width. */
export const MINIMAP_RAIL_WIDTH_RATIO = 0.20;

/** Minimum rail width in points (small iPhones). */
export const MINIMAP_RAIL_MIN_WIDTH = 72;

/** Maximum rail width in points (large iPhones). */
export const MINIMAP_RAIL_MAX_WIDTH = 84;

/**
 * Vertical gap between every pair of adjacent MiniMap boxes (counter, turns, footer).
 * Applied as half-gap above and below each turn item so all spacing is equal.
 */
export const MINIMAP_BOX_GAP = 6;

/** Height of each MiniMap turn box (the inner box, not the touch target). */
export const MINIMAP_BOX_HEIGHT = 26;
