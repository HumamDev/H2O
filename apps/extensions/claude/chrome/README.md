# apps/extensions/claude/chrome — FUTURE PLACEHOLDER

Future Chrome MV3 extension build target for `claude.ai`.

Not yet implemented. The H2O extension stack currently targets only
`chatgpt.com` (under `apps/extensions/chatgpt/chrome/`).

This folder reserves the spot for a future Claude-targeted Chrome
extension. Until then this README is the only tracked content here;
the folder contains no build pipeline, no source, no manifest, and
no host-adapter logic.
