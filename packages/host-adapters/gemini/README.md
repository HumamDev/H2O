# packages/host-adapters/gemini — FUTURE PLACEHOLDER

Future Gemini-specific host adapter — `gemini.google.com` DOM
selectors, observer rules, chat-tree parsing, and other Gemini-specific
glue.

Not yet implemented. Companion to `apps/extensions/gemini/chrome/` (also
a future placeholder). See `packages/extension-core/README.md` for the
broader cross-host adapter design rationale.

Until then this README is the only tracked content; no package.json,
no source, no workspace entry.
