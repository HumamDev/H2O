# packages/host-adapters/claude — FUTURE PLACEHOLDER

Future Claude-specific host adapter — `claude.ai` DOM selectors,
observer rules, chat-tree parsing, and other Claude-specific glue.

Not yet implemented. Companion to `apps/extensions/claude/chrome/` (also
a future placeholder). See `packages/extension-core/README.md` for the
broader cross-host adapter design rationale.

Until then this README is the only tracked content; no package.json,
no source, no workspace entry.
